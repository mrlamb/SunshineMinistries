<Window x:Class="ContactAppWPF.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactAppWPF.Views"
        mc:Ignorable="d"
        Title="Settings" Height="450" Width="400" Topmost="True" 
        >
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListBox SelectedItem="{Binding ListSelectedItem}" BorderThickness="1" Margin="5" Grid.Column="0" Grid.Row="0" MinHeight="100">
            <ListBoxItem Content="General"/>
        </ListBox>
        <ContentControl x:Name="ActiveItem" Grid.Column="1" Grid.Row="0"/>
        <Button x:Name="TryClose" Content="Close" Grid.Column="0" Grid.Row="1"/>
    </Grid>
</Window>
